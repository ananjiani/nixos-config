---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: renovate
  namespace: renovate
spec:
  schedule: "0 5 * * *" # Daily 5am (matches renovate.json schedule)
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: renovate
              image: renovate/renovate:full@sha256:a254972f5304274b8e721dcd676b0afc65f52794c5696dd9ff54f94b5a5806a1
              env:
                # Dynamically install manager-specific tools that are not bundled
                # in renovate:full, such as the Flux CLI used to regenerate
                # k8s/flux-system/gotk-components.yaml.
                - name: RENOVATE_BINARY_SOURCE
                  value: "install"
                - name: RENOVATE_PLATFORM
                  value: "forgejo"
                - name: RENOVATE_ENDPOINT
                  value: "https://codeberg.org"
                - name: RENOVATE_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: renovate-credentials
                      key: token
                - name: RENOVATE_GIT_AUTHOR
                  value: "Renovate <ananjiani@noreply.codeberg.org>"
                - name: RENOVATE_REPOSITORIES
                  value: "ananjiani/infra"
                - name: LOG_LEVEL
                  value: "info"
                # GitHub token for fetching release notes from GitHub-hosted deps
                - name: GITHUB_COM_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: renovate-credentials
                      key: github-token
              resources:
                requests:
                  memory: "512Mi"
                  cpu: "200m"
                limits:
                  memory: "1Gi"
                  cpu: "1000m"
          restartPolicy: OnFailure
