---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cliproxy-tls
  namespace: cliproxy
spec:
  secretName: cliproxy-tls
  issuerRef:
    name: lan-ca
    kind: ClusterIssuer
  dnsNames:
    - cliproxy.lan
