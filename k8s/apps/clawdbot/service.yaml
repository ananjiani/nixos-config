---
apiVersion: v1
kind: Service
metadata:
  name: clawdbot
  namespace: clawdbot
spec:
  type: ClusterIP
  selector:
    app: clawdbot
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
