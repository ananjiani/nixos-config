---
# Service for external Attic (no selector - uses endpoints)
apiVersion: v1
kind: Service
metadata:
  name: attic-external
  namespace: attic
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
