---
apiVersion: v1
kind: Service
metadata:
  name: voicemail-receiver
  namespace: voicemail-receiver
spec:
  type: ClusterIP  # Internal only - exposed via IngressRoute
  selector:
    app: voicemail-receiver
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
