---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: voicemail-lan-tls
  namespace: voicemail-receiver
spec:
  secretName: voicemail-lan-tls
  issuerRef:
    name: lan-ca
    kind: ClusterIssuer
  dnsNames:
    - voicemail.lan
