secrets/emacs/emacs
secrets/emacs/emacs.pub
secrets/work-vpn-d.conf
result
.direnv
.pre-commit-config.yaml

# Unencrypted secrets for testing (NEVER commit these!)
secrets/*-unencrypted.yaml

# Backups
portainer-backup*
backups/
